angular.module('ngdir', [])
    .directive('datepicker',
        function() { 
            return {
                restrict: 'AE',
                replace: true,
                template: '<input type="text" />',
                link: function(scope, elem, attrs) {
                }
            }; 
        });
